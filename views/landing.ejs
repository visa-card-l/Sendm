<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>
  <style>
    :root {
      --primary: #1564C0;
      --primary-light: #3485e5;
    }
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }
    body {
      font-family: 'Segoe UI', system-ui, Arial, sans-serif;
      background: #FFFFFF;
      color: #343a40;
      line-height: 1.7;
    }
    .container {
      max-width: 700px;
      margin: 0 auto;
      background: #FFFFFF;
      border-radius: 24px;
      overflow: hidden;
      box-shadow: 0 20px 60px rgba(0,0,0,0.08);
      display: flex;
      flex-direction: column;
      min-height: 100vh;
    }
    .content {
      padding: 20px 50px 60px;
      text-align: center;
      background: #FFFFFF;
      flex: 1;
    }
    h1 {
      font-size: 28px;
      line-height: 1.3;
      font-weight: 700;
      margin-bottom: 15px;
      background: linear-gradient(135deg, var(--primary), var(--primary-light));
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
    }
    h2 {
      font-size: 26px;
      font-weight: 700;
      margin: 30px 0 15px;
      color: #1a1a1a;
    }
    p {
      margin-bottom: 1.6em; /* Slightly increased for natural paragraph spacing */
    }
    .hero-img {
      max-width: 100%;
      border-radius: 18px;
      box-shadow: 0 15px 40px rgba(0,0,0,0.15);
      margin: 60px 0; /* Reduced from 100px – moderate separation */
    }
    .cta {
      display: inline-block;
      padding: 8px 24px;
      font-size: 13px;
      font-weight: 600;
      background: var(--primary);
      color: white;
      border-radius: 50px;
      box-shadow: 0 3px 10px rgba(21,100,192,0.25);
      text-decoration: none;
      margin: 40px 0; /* Reduced from 70px – moderate top/bottom separation */
      transition: all 0.2s ease;
      font-family: system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
    }
    .cta:hover {
      background: var(--primary-light);
      transform: translateY(-1px);
      box-shadow: 0 5px 14px rgba(21,100,192,0.35);
    }
    .form-block {
      padding: 40px;
      background: #FFFFFF;
      border-radius: 20px;
      margin: 60px 0; /* Reduced from 100px – moderate separation */
      border: 1px solid #e0e7ff;
    }
    .form-block input,
    .form-block button {
      width: 100%;
      padding: 16px;
      margin: 10px 0;
      border-radius: 12px;
      border: 1px solid #ddd;
      font-size: 16px;
    }
    .form-block button {
      background: var(--primary);
      color: white;
      border: none;
      font-weight: 600;
      cursor: pointer;
    }
    .footer-cta {
      padding: 40px 50px;
      text-align: center;
      background: #FFFFFF;
      margin-top: auto;
    }
    .join-sendmi-btn {
      display: inline-block;
      padding: 8px 24px;
      font-size: 13px;
      font-weight: 600;
      background: #0066FF;
      color: white;
      border-radius: 50px;
      text-decoration: none;
      box-shadow: 0 3px 10px rgba(0, 102, 255, 0.25);
      transition: all 0.2s ease;
      font-family: system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
    }
    .join-sendmi-btn:hover {
      background: #0055dd;
      transform: translateY(-1px);
      box-shadow: 0 5px 14px rgba(0, 102, 255, 0.35);
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="content">
      <% blocks.forEach(block => { %>
        <% if (block.type === 'text') { %>
          <% 
            let html = block.content
              .replace(/\n{2,}/g, '</p><p>')
              .replace(/\n/g, '<br>');
            
            if (!html.includes('<p')) {
              html = '<p>' + html + '</p>';
            }
          %>

          <% if (block.tag === 'h1') { %>
            <h1><%- html %></h1>
          <% } else if (block.tag === 'h2') { %>
            <h2><%- html %></h2>
          <% } else { %>
            <%- html %>
          <% } %>
        <% } else if (block.type === 'image') { %>
          <img src="<%= block.src %>" alt="Image" class="hero-img" loading="lazy">
        <% } else if (block.type === 'button') { %>
          <a href="<%= block.href || '#' %>" class="cta" <%= block.href && block.href.startsWith('http') ? 'target="_blank" rel="noopener"' : '' %>><%= block.text %></a>
        <% } else if (block.type === 'form') { %>
          <div class="form-block"><%- block.html %></div>
        <% } %>
      <% }) %>
    </div>

    <!-- Very small button at the absolute bottom -->
    <div class="footer-cta">
      <a href="https://sendmi.onrender.com" class="join-sendmi-btn" target="_blank" rel="noopener">
        Join Sendmi
      </a>
    </div>
  </div>
</body>
</html>
