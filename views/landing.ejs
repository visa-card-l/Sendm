<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>
  <style>
    :root {
      --primary: #1564C0;
      --primary-light: #3485e5;
      --gap-tiny: 8px;
      --gap-small: 12px;
      --gap-after-heading: 12px;
    }
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }
    body {
      font-family: 'Segoe UI', system-ui, Arial, sans-serif;
      background: #FFFFFF;
      color: #343a40;
      line-height: 1.55;
    }
    .container {
      max-width: 700px;
      margin: 0 auto;
      background: #FFFFFF;
      border-radius: 24px;
      overflow: hidden;
      box-shadow: 0 20px 60px rgba(0,0,0,0.08);
      display: flex;
      flex-direction: column;
      min-height: 100vh;
    }
    .content {
      padding: 24px 44px 40px;
      background: #FFFFFF;
      flex: 1;
      display: flex;
      flex-direction: column;
      align-items: flex-end;
      gap: var(--gap-small);
    }
    h1 {
      font-size: 28px;
      line-height: 1.3;
      font-weight: 700;
      margin: 0 auto 16px;
      background: linear-gradient(135deg, var(--primary), var(--primary-light));
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
      align-self: center;
    }
    h2 {
      font-size: 26px;
      font-weight: 700;
      margin: 0 auto 16px;
      color: #1a1a1a;
      align-self: center;
    }
    p {
      margin-bottom: 0.9em;
      max-width: 100%;
    }

    /* Tight chaining rules */
    h1 + *, h2 + * {
      margin-top: var(--gap-after-heading) !important;
    }
    .hero-img + *,
    * + .hero-img,
    * + .cta,
    .cta + *,
    * + .form-block,
    .form-block + * {
      margin-top: var(--gap-tiny) !important;
    }

    .hero-img {
      max-width: 100%;
      border-radius: 18px;
      box-shadow: 0 15px 40px rgba(0,0,0,0.15);
      margin: var(--gap-small) 0 var(--gap-tiny);
      align-self: center;
    }
    .cta {
      display: block;
      width: fit-content;
      padding: 8px 24px;
      font-size: 13px;
      font-weight: 600;
      background: var(--primary);
      color: white;
      border-radius: 50px;
      box-shadow: 0 3px 10px rgba(21,100,192,0.25);
      text-decoration: none;
      margin: var(--gap-tiny) 0;
      transition: all 0.2s ease;
    }
    .cta:hover {
      background: var(--primary-light);
      transform: translateY(-1px);
      box-shadow: 0 5px 14px rgba(21,100,192,0.35);
    }
    .form-block {
      padding: 24px;
      background: #FFFFFF;
      border-radius: 20px;
      margin: var(--gap-small) 0;
      border: 1px solid #e0e7ff;
      width: 100%;
      align-self: stretch;
    }
    .form-block input,
    .form-block button {
      width: 100%;
      padding: 14px;
      margin: 8px 0;
      border-radius: 12px;
      border: 1px solid #ddd;
      font-size: 16px;
    }
    .form-block button {
      background: var(--primary);
      color: white;
      border: none;
      font-weight: 600;
      cursor: pointer;
    }
    .footer-cta {
      padding: 20px 44px;
      text-align: center;
      background: #FFFFFF;
      margin-top: auto;
      font-size: 13px;           /* slightly smaller than body text */
      color: #666;
    }

    /* New tiny underlined link style for footer */
    .footer-link {
      color: var(--primary);
      text-decoration: underline;
      text-underline-offset: 2px;
      font-size: 12px;           /* tiny size */
      font-weight: 500;
      transition: color 0.2s ease;
    }

    .footer-link:hover,
    .footer-link:focus {
      color: var(--primary-light);
      text-decoration-thickness: 1.5px; /* optional: slightly thicker on hover */
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="content">
      <% blocks.forEach(block => { %>
        <% if (block.type === 'text') { %>
          <% 
            let html = block.content
              .replace(/\n{2,}/g, '</p><p>')
              .replace(/\n/g, '<br>');
            
            if (!html.includes('<p')) {
              html = '<p>' + html + '</p>';
            }
          %>

          <% if (block.tag === 'h1') { %>
            <h1><%- html %></h1>
          <% } else if (block.tag === 'h2') { %>
            <h2><%- html %></h2>
          <% } else { %>
            <%- html %>
          <% } %>
        <% } else if (block.type === 'image') { %>
          <img src="<%= block.src %>" alt="Image" class="hero-img" loading="lazy">
        <% } else if (block.type === 'button') { %>
          <a href="<%= block.href || '#' %>" 
             class="cta"
             <%= block.href && block.href.startsWith('http') ? 'target="_blank" rel="noopener"' : '' %>>
            <%= block.text %>
          </a>
        <% } else if (block.type === 'form') { %>
          <div class="form-block"><%- block.html %></div>
        <% } %>
      <% }) %>
    </div>

    <div class="footer-cta">
      <a href="https://sendmi.onrender.com" 
         class="footer-link" 
         target="_blank" 
         rel="noopener">
        Join Sendmi
      </a>
    </div>
  </div>
</body>
</html>
