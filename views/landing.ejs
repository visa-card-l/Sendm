<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>
  <style>
    :root {
      --primary: #1564C0;
      --primary-light: #3485e5;
      --gap-blocks: 20px;
      --gap-small: 12px;
    }
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }
    body {
      font-family: 'Segoe UI', system-ui, Arial, sans-serif;
      background: #FFFFFF;
      color: #343a40;
      line-height: 1.55;
    }
    .container {
      max-width: 720px;
      margin: 0 auto;
      background: #FFFFFF;
      border-radius: 20px;
      overflow: hidden;
      box-shadow: 0 16px 48px rgba(0,0,0,0.07);
      display: flex;
      flex-direction: column;
      min-height: 100vh;
    }
    .content {
      padding: 24px 44px 36px;
      flex: 1;
      display: flex;
      flex-direction: column;
      align-items: flex-end;
      gap: var(--gap-blocks);           /* main vertical spacing between different blocks */
    }
    h1 {
      font-size: 26px;
      font-weight: 700;
      margin-bottom: 8px;
      background: linear-gradient(135deg, var(--primary), var(--primary-light));
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      align-self: center;
    }
    h2 {
      font-size: 22px;
      font-weight: 700;
      margin: 16px 0 8px;
      color: #1a1a1a;
      align-self: center;
    }
    p {
      margin-bottom: 0.9em;
    }
    .hero-img {
      max-width: 100%;
      border-radius: 14px;
      box-shadow: 0 10px 24px rgba(0,0,0,0.1);
      align-self: center;
      margin: 8px 0;                    /* very tight around images */
    }
    .cta {
      display: block;
      width: fit-content;
      padding: 9px 24px;
      font-size: 13.5px;
      font-weight: 600;
      background: var(--primary);
      color: white;
      border-radius: 50px;
      box-shadow: 0 2px 8px rgba(21,100,192,0.22);
      text-decoration: none;
      transition: all 0.18s ease;
      margin: 4px 0;                    /* almost no extra space around buttons */
    }
    .cta:hover {
      background: var(--primary-light);
      transform: translateY(-1px);
      box-shadow: 0 5px 14px rgba(21,100,192,0.32);
    }
    .form-block {
      padding: 24px;
      background: #FFFFFF;
      border-radius: 14px;
      border: 1px solid #e0e7ff;
      width: 100%;
      align-self: stretch;
      margin: 6px 0;
    }
    .form-block input,
    .form-block button {
      width: 100%;
      padding: 13px 15px;
      margin: 8px 0;
      border-radius: 10px;
      border: 1px solid #ddd;
      font-size: 15px;
    }
    .form-block button {
      background: var(--primary);
      color: white;
      border: none;
      font-weight: 600;
      cursor: pointer;
      transition: background 0.18s;
    }
    .form-block button:hover {
      background: var(--primary-light);
    }
    .footer-cta {
      padding: 24px 44px;
      background: #FFFFFF;
      margin-top: auto;
      text-align: center;
    }
    .join-sendmi-btn {
      display: inline-block;
      padding: 9px 24px;
      font-size: 13.5px;
      font-weight: 600;
      background: #0066FF;
      color: white;
      border-radius: 50px;
      text-decoration: none;
      box-shadow: 0 2px 8px rgba(0, 102, 255, 0.22);
      transition: all 0.18s ease;
    }
    .join-sendmi-btn:hover {
      background: #0055dd;
      transform: translateY(-1px);
      box-shadow: 0 5px 14px rgba(0, 102, 255, 0.32);
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="content">
      <% blocks.forEach(block => { %>
        <% if (block.type === 'text') { %>
          <% 
            let html = block.content
              .replace(/\n{2,}/g, '</p><p>')
              .replace(/\n/g, '<br>');
            
            if (!html.includes('<p')) {
              html = '<p>' + html + '</p>';
            }
          %>

          <% if (block.tag === 'h1') { %>
            <h1><%- html %></h1>
          <% } else if (block.tag === 'h2') { %>
            <h2><%- html %></h2>
          <% } else { %>
            <%- html %>
          <% } %>
        <% } else if (block.type === 'image') { %>
          <img src="<%= block.src %>" alt="Image" class="hero-img" loading="lazy">
        <% } else if (block.type === 'button') { %>
          <a href="<%= block.href || '#' %>" 
             class="cta"
             <%= block.href && block.href.startsWith('http') ? 'target="_blank" rel="noopener"' : '' %>>
            <%= block.text %>
          </a>
        <% } else if (block.type === 'form') { %>
          <div class="form-block"><%- block.html %></div>
        <% } %>
      <% }) %>
    </div>

    <div class="footer-cta">
      <a href="https://sendmi.onrender.com" class="join-sendmi-btn" target="_blank" rel="noopener">
        Join Sendmi
      </a>
    </div>
  </div>
</body>
</html>
