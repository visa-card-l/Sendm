<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>
  <style>
    :root {
      --primary: #1564C0;
      --primary-light: #3485e5;
      --spacing-xxs: 8px;
      --spacing-xs: 14px;
      --spacing-sm: 20px;
      --spacing-md: 32px;
      --spacing-lg: 48px;
      --spacing-xl: 64px;
    }

    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: 'Segoe UI', system-ui, -apple-system, sans-serif;
      background: #f8f9fa;
      color: #1f2937;
      line-height: 1.6;
    }

    .container {
      max-width: 760px;
      margin: 0 auto;
      background: white;
      border-radius: 20px;
      overflow: hidden;
      box-shadow: 0 10px 40px rgba(0,0,0,0.07);
      min-height: 100vh;
      display: flex;
      flex-direction: column;
    }

    .content {
      padding: 48px 28px 72px;
      flex: 1;
    }

    /* ── Very prominent header ── */
    h1 {
      font-size: clamp(48px, 9.5vw, 76px);   /* scales nicely on mobile → desktop */
      font-weight: 900;
      line-height: 1.15;
      letter-spacing: -1.2px;
      text-align: center;
      margin: 0 auto 64px auto;
      max-width: 92%;
      background: linear-gradient(135deg, #0f62fe, var(--primary-light), #60a5fa);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
      text-shadow: 0 4px 16px rgba(21, 100, 192, 0.18);
      padding: 0 12px;
    }

    h2 {
      font-size: clamp(32px, 6vw, 44px);
      font-weight: 800;
      line-height: 1.22;
      margin: 0 auto 48px;
      max-width: 90%;
      color: #111827;
      text-align: center;
      letter-spacing: -0.5px;
    }

    p {
      margin-bottom: 1.5em;
      font-size: 1.05rem;
      color: #374151;
    }

    /* Give big header lots of space */
    .content > h1 {
      margin-top: 0;
      margin-bottom: 72px;
    }

    .content > h1 + * {
      margin-top: 0 !important;
    }

    /* Slightly more separation after h2 */
    .content > h2 + * {
      margin-top: 40px !important;
    }

    /* Tight chaining for non-header elements */
    .hero-img + h1,
    .hero-img + h2,
    .hero-img + p,
    h1 + p,
    h2 + p,
    p + .cta-wrapper,
    .cta-wrapper + h2,
    .cta-wrapper + .hero-img,
    .cta-wrapper + p,
    .form-block + .hero-img,
    .form-block + h2,
    p + h2,
    p + .hero-img,
    p + .form-block,
    .hero-img + .form-block,
    .hero-img + .cta-wrapper {
      margin-top: var(--spacing-xxs) !important;
    }

    .hero-img {
      width: 100%;
      border-radius: 14px;
      box-shadow: 0 10px 28px rgba(0,0,0,0.11);
      margin: var(--spacing-sm) 0 var(--spacing-xs);
    }

    .cta-wrapper {
      text-align: right;
      margin: var(--spacing-xs) 0 var(--spacing-md);
    }

    .cta {
      display: inline-block;
      padding: 14px 36px;
      font-size: 1.05rem;
      font-weight: 600;
      background: var(--primary);
      color: white;
      border-radius: 999px;
      text-decoration: none;
      box-shadow: 0 4px 14px rgba(21,100,192,0.22);
      transition: all 0.24s ease;
    }

    .cta:hover {
      background: var(--primary-light);
      transform: translateY(-3px);
      box-shadow: 0 10px 32px rgba(21,100,192,0.35);
    }

    .form-block {
      padding: 32px;
      background: #ffffff;
      border-radius: 16px;
      margin: var(--spacing-lg) 0;
      border: 1px solid #e5e7eb;
      box-shadow: 0 4px 16px rgba(0,0,0,0.05);
    }

    .form-block input,
    .form-block button {
      width: 100%;
      padding: 14px 16px;
      margin: var(--spacing-xxs) 0;
      border-radius: 10px;
      border: 1px solid #d1d5db;
      font-size: 1.03rem;
    }

    .form-block button {
      background: var(--primary);
      color: white;
      border: none;
      font-weight: 600;
      cursor: pointer;
      margin-top: var(--spacing-sm);
    }

    .footer-cta {
      padding: 16px 28px;
      text-align: center;
      background: #f8fafc;
      border-top: 1px solid #e5e7eb;
      font-size: 0.84rem;
      color: #6b7280;
    }

    .join-sendmi-link {
      color: #3b82f6;
      text-decoration: underline;
      font-weight: 400;
      font-size: 0.78rem;
      transition: color 0.18s ease;
    }

    .join-sendmi-link:hover,
    .join-sendmi-link:focus {
      color: var(--primary);
      text-decoration-thickness: 2px;
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="content">
      <% blocks.forEach(block => { %>
        <% if (block.type === 'text') { %>
          <% 
            let html = block.content
              .replace(/\n{2,}/g, '</p><p>')
              .replace(/\n/g, '<br>');
            
            if (!html.includes('<p')) {
              html = '<p>' + html + '</p>';
            }
          %>

          <% if (block.tag === 'h1') { %>
            <h1><%- html %></h1>
          <% } else if (block.tag === 'h2') { %>
            <h2><%- html %></h2>
          <% } else { %>
            <%- html %>
          <% } %>
        <% } else if (block.type === 'image') { %>
          <img src="<%= block.src %>" alt="Image" class="hero-img" loading="lazy">
        <% } else if (block.type === 'button') { %>
          <div class="cta-wrapper">
            <a href="<%= block.href || '#' %>" class="cta" 
               <%= block.href && block.href.startsWith('http') ? 'target="_blank" rel="noopener"' : '' %>>
              <%= block.text %>
            </a>
          </div>
        <% } else if (block.type === 'form') { %>
          <div class="form-block"><%- block.html %></div>
        <% } %>
      <% }) %>
    </div>

    <div class="footer-cta">
      <a href="https://sendmi.onrender.com" class="join-sendmi-link" target="_blank" rel="noopener">
        Join Sendmi
      </a>
    </div>
  </div>
</body>
</html>
