<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>
  <style>
    :root {
      --primary: #1564C0;
      --primary-light: #3485e5;
    }
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }
    body {
      font-family: 'Segoe UI', system-ui, Arial, sans-serif;
      background: #f8f9fa;
      color: #1a1a1a;
      line-height: 1.5;
    }
    .container {
      max-width: 700px;
      margin: 0 auto;
      padding: 20px;
      background: #FFFFFF;
      min-height: 100vh;
    }

    /* Product card wrapper – each item gets its own card */
    .product-card {
      background: white;
      border-radius: 16px;
      overflow: hidden;
      box-shadow: 0 4px 16px rgba(0,0,0,0.08);
      margin-bottom: 24px;
      transition: transform 0.15s ease, box-shadow 0.15s ease;
    }

    .product-card:hover {
      transform: translateY(-4px);
      box-shadow: 0 12px 32px rgba(0,0,0,0.12);
    }

    .content {
      padding: 0; /* moved padding to card */
    }

    /* Image takes full width, tight spacing */
    .hero-img {
      width: 100%;
      height: auto;
      display: block;
      object-fit: cover;
      aspect-ratio: 4 / 3; /* ← adjust this ratio to match your product images */
    }

    /* Text content – very compact */
    .product-info {
      padding: 16px 20px 20px;
      text-align: left;
    }

    h1 {
      font-size: 24px;
      font-weight: 700;
      margin: 0 0 8px 0;
      background: linear-gradient(135deg, var(--primary), var(--primary-light));
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
    }

    h2 {
      font-size: 20px;
      font-weight: 600;
      margin: 0 0 6px 0;
      color: #1a1a1a;
    }

    p {
      font-size: 15px;
      color: #555;
      margin: 0 0 16px 0;
      line-height: 1.45;
    }

    /* Button – tight spacing, full-width option */
    .cta {
      display: inline-block;
      padding: 12px 28px;
      font-size: 15px;
      font-weight: 600;
      background: var(--primary);
      color: white;
      border-radius: 50px;
      text-decoration: none;
      transition: all 0.2s ease;
    }

    .cta:hover {
      background: var(--primary-light);
      transform: translateY(-1px);
    }

    .cta.full-width {
      display: block;
      text-align: center;
      margin: 12px 0 4px;
    }

    /* Footer remains small */
    .footer-cta {
      padding: 32px 20px;
      text-align: center;
    }

    .join-sendmi-btn {
      padding: 10px 28px;
      font-size: 14px;
      background: #0066FF;
      color: white;
      border-radius: 50px;
      text-decoration: none;
      box-shadow: 0 3px 12px rgba(0,102,255,0.25);
      transition: all 0.2s;
    }

    .join-sendmi-btn:hover {
      background: #0055dd;
      transform: translateY(-1px);
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="content">
      <% blocks.forEach(block => { %>
        <% if (block.type === 'text') { %>
          <% 
            let html = block.content
              .replace(/\n{2,}/g, '</p><p>')
              .replace(/\n/g, '<br>');
            
            if (!html.includes('<p')) {
              html = '<p>' + html + '</p>';
            }
          %>

          <% if (block.tag === 'h1') { %>
            <h1><%- html %></h1>
          <% } else if (block.tag === 'h2') { %>
            <h2><%- html %></h2>
          <% } else { %>
            <%- html %>
          <% } %>
        <% } else if (block.type === 'image') { %>
          <img src="<%= block.src %>" alt="Product" class="hero-img" loading="lazy">
        <% } else if (block.type === 'button') { %>
          <a href="<%= block.href || '#' %>" 
             class="cta <%= block.fullWidth ? 'full-width' : '' %>" 
             <%= block.href && block.href.startsWith('http') ? 'target="_blank" rel="noopener"' : '' %>>
            <%= block.text %>
          </a>
        <% } else if (block.type === 'form') { %>
          <div style="padding: 20px;"><%- block.html %></div>
        <% } %>
      <% }) %>
    </div>

    <div class="footer-cta">
      <a href="https://sendmi.onrender.com" class="join-sendmi-btn" target="_blank" rel="noopener">
        Join Sendmi
      </a>
    </div>
  </div>
</body>
</html>
