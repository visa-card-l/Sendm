<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>
  <style>
    :root {
      --primary: #1564C0;
      --primary-light: #3485e5;
      --spacing-sm: 20px;
      --spacing-md: 36px;
    }
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }
    body {
      font-family: 'Segoe UI', system-ui, Arial, sans-serif;
      background: #FFFFFF;
      color: #343a40;
      line-height: 1.6;
    }
    .container {
      max-width: 700px;
      margin: 0 auto;
      background: #FFFFFF;
      border-radius: 24px;
      overflow: hidden;
      box-shadow: 0 20px 60px rgba(0,0,0,0.08);
      display: flex;
      flex-direction: column;
      min-height: 100vh;
    }
    .content {
      padding: 32px 50px 48px;
      background: #FFFFFF;
      flex: 1;

      display: flex;
      flex-direction: column;
      align-items: flex-end;
      gap: var(--spacing-md);          /* ← main spacing between blocks */
    }
    h1 {
      font-size: 28px;
      font-weight: 700;
      margin-bottom: 12px;
      background: linear-gradient(135deg, var(--primary), var(--primary-light));
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
      align-self: center;
    }
    h2 {
      font-size: 24px;
      font-weight: 700;
      margin: 24px 0 12px;
      color: #1a1a1a;
      align-self: center;
    }
    p {
      margin-bottom: 1.2em;
      max-width: 100%;
    }
    .hero-img {
      max-width: 100%;
      border-radius: 16px;
      box-shadow: 0 12px 32px rgba(0,0,0,0.12);
      align-self: center;
      margin: var(--spacing-sm) 0;
    }
    .cta {
      display: block;
      width: fit-content;
      padding: 10px 28px;
      font-size: 14px;
      font-weight: 600;
      background: var(--primary);
      color: white;
      border-radius: 50px;
      box-shadow: 0 3px 10px rgba(21,100,192,0.25);
      text-decoration: none;
      transition: all 0.2s ease;
      font-family: system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
      margin: 12px 0;                    /* much smaller vertical space around buttons */
    }
    .cta:hover {
      background: var(--primary-light);
      transform: translateY(-1px);
      box-shadow: 0 6px 16px rgba(21,100,192,0.35);
    }
    .form-block {
      padding: 32px;
      background: #FFFFFF;
      border-radius: 16px;
      border: 1px solid #e0e7ff;
      width: 100%;
      align-self: stretch;
      margin: var(--spacing-sm) 0;
    }
    .form-block input,
    .form-block button {
      width: 100%;
      padding: 14px 16px;
      margin: 10px 0;
      border-radius: 10px;
      border: 1px solid #ddd;
      font-size: 16px;
    }
    .form-block button {
      background: var(--primary);
      color: white;
      border: none;
      font-weight: 600;
      cursor: pointer;
      transition: background 0.2s;
    }
    .form-block button:hover {
      background: var(--primary-light);
    }
    .footer-cta {
      padding: 32px 50px;
      background: #FFFFFF;
      margin-top: auto;
      text-align: center;
    }
    .join-sendmi-btn {
      display: inline-block;
      padding: 10px 28px;
      font-size: 14px;
      font-weight: 600;
      background: #0066FF;
      color: white;
      border-radius: 50px;
      text-decoration: none;
      box-shadow: 0 3px 10px rgba(0, 102, 255, 0.25);
      transition: all 0.2s ease;
    }
    .join-sendmi-btn:hover {
      background: #0055dd;
      transform: translateY(-1px);
      box-shadow: 0 6px 16px rgba(0, 102, 255, 0.35);
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="content">
      <% blocks.forEach(block => { %>
        <% if (block.type === 'text') { %>
          <% 
            let html = block.content
              .replace(/\n{2,}/g, '</p><p>')
              .replace(/\n/g, '<br>');
            
            if (!html.includes('<p')) {
              html = '<p>' + html + '</p>';
            }
          %>

          <% if (block.tag === 'h1') { %>
            <h1><%- html %></h1>
          <% } else if (block.tag === 'h2') { %>
            <h2><%- html %></h2>
          <% } else { %>
            <%- html %>
          <% } %>
        <% } else if (block.type === 'image') { %>
          <img src="<%= block.src %>" alt="Image" class="hero-img" loading="lazy">
        <% } else if (block.type === 'button') { %>
          <a href="<%= block.href || '#' %>" 
             class="cta"
             <%= block.href && block.href.startsWith('http') ? 'target="_blank" rel="noopener"' : '' %>>
            <%= block.text %>
          </a>
        <% } else if (block.type === 'form') { %>
          <div class="form-block"><%- block.html %></div>
        <% } %>
      <% }) %>
    </div>

    <div class="footer-cta">
      <a href="https://sendmi.onrender.com" class="join-sendmi-btn" target="_blank" rel="noopener">
        Join Sendmi
      </a>
    </div>
  </div>
</body>
</html>
