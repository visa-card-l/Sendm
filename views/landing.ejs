<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>
  <style>
    :root {
      --primary: #1564C0;
      --primary-light: #3485e5;
      --spacing-xxs: 8px;
      --spacing-xs: 14px;
      --spacing-sm: 20px;
      --spacing-md: 32px;
      --spacing-lg: 48px;
    }

    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: 'Segoe UI', system-ui, Arial, sans-serif;
      background: #f8f9fa;
      color: #1f2937;
      line-height: 1.6;
    }

    .container {
      max-width: 740px;
      margin: 0 auto;
      background: white;
      border-radius: 20px;
      overflow: hidden;
      box-shadow: 0 10px 40px rgba(0,0,0,0.07);
      min-height: 100vh;
      display: flex;
      flex-direction: column;
    }

    .content {
      padding: 28px 28px 64px;
      flex: 1;
    }

    h1 {
      font-size: 2.1rem;
      font-weight: 800;
      line-height: 1.22;
      margin-bottom: var(--spacing-xs);
      background: linear-gradient(135deg, var(--primary), var(--primary-light));
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
    }

    h2 {
      font-size: 1.7rem;
      font-weight: 700;
      margin: var(--spacing-sm) 0 var(--spacing-xxs);
      color: #111827;
    }

    p {
      margin-bottom: 1.25em;
      font-size: 1.04rem;
      color: #374151;
    }

    .hero-img {
      width: 100%;
      border-radius: 14px;
      box-shadow: 0 10px 28px rgba(0,0,0,0.11);
      margin: var(--spacing-sm) 0 var(--spacing-xs);
    }

    /* Right-aligned CTA wrapper */
    .cta-wrapper {
      text-align: right;
      margin: var(--spacing-xs) 0 var(--spacing-sm);
    }

    .cta {
      display: inline-block;
      padding: 13px 30px;
      font-size: 1rem;
      font-weight: 600;
      background: var(--primary);
      color: white;
      border-radius: 999px;
      text-decoration: none;
      box-shadow: 0 4px 12px rgba(21,100,192,0.2);
      transition: all 0.22s ease;
    }

    .cta:hover {
      background: var(--primary-light);
      transform: translateY(-2px);
      box-shadow: 0 8px 28px rgba(21,100,192,0.32); /* Slightly stronger on hover */
    }

    .form-block {
      padding: 28px;
      background: #ffffff;
      border-radius: 14px;
      margin: var(--spacing-md) 0;
      border: 1px solid #e5e7eb;
      box-shadow: 0 3px 14px rgba(0,0,0,0.04);
    }

    .form-block input,
    .form-block button {
      width: 100%;
      padding: 13px 15px;
      margin: var(--spacing-xxs) 0;
      border-radius: 10px;
      border: 1px solid #d1d5db;
      font-size: 1rem;
    }

    .form-block button {
      background: var(--primary);
      color: white;
      border: none;
      font-weight: 600;
      cursor: pointer;
      margin-top: var(--spacing-xs);
    }

    .footer-cta {
      padding: 28px 28px;
      text-align: center;
      background: #f8fafc;
      border-top: 1px solid #e5e7eb;
    }

    .join-sendmi-btn {
      display: inline-block;
      padding: 10px 26px;
      font-size: 0.94rem;
      font-weight: 600;
      background: #0066FF;
      color: white;
      border-radius: 999px;
      text-decoration: none;
      box-shadow: 0 4px 12px rgba(0,102,255,0.18);
      transition: all 0.22s ease;
    }

    .join-sendmi-btn:hover {
      background: #0055dd;
      transform: translateY(-1px);
      box-shadow: 0 7px 20px rgba(0,102,255,0.28);
    }

    /* Tighter chaining between related elements */
    .hero-img + h1,
    .hero-img + h2,
    .hero-img + p,
    h1 + p,
    h2 + p,
    p + .cta-wrapper,
    .cta-wrapper + h2,
    .cta-wrapper + .hero-img,
    .cta-wrapper + p,
    .form-block + .hero-img,
    .form-block + h2 {
      margin-top: var(--spacing-xxs) !important;
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="content">
      <% blocks.forEach(block => { %>
        <% if (block.type === 'text') { %>
          <% 
            let html = block.content
              .replace(/\n{2,}/g, '</p><p>')
              .replace(/\n/g, '<br>');
            
            if (!html.includes('<p')) {
              html = '<p>' + html + '</p>';
            }
          %>

          <% if (block.tag === 'h1') { %>
            <h1><%- html %></h1>
          <% } else if (block.tag === 'h2') { %>
            <h2><%- html %></h2>
          <% } else { %>
            <%- html %>
          <% } %>
        <% } else if (block.type === 'image') { %>
          <img src="<%= block.src %>" alt="Image" class="hero-img" loading="lazy">
        <% } else if (block.type === 'button') { %>
          <div class="cta-wrapper">
            <a href="<%= block.href || '#' %>" class="cta" 
               <%= block.href && block.href.startsWith('http') ? 'target="_blank" rel="noopener"' : '' %>>
              <%= block.text %>
            </a>
          </div>
        <% } else if (block.type === 'form') { %>
          <div class="form-block"><%- block.html %></div>
        <% } %>
      <% }) %>
    </div>

    <div class="footer-cta">
      <a href="https://sendmi.onrender.com" class="join-sendmi-btn" target="_blank" rel="noopener">
        Join Sendmi
      </a>
    </div>
  </div>
</body>
</html>
