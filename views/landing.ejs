<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>
  <style>
    :root {
      --primary: #1564C0;
      --primary-light: #3485e5;
      --primary-dark: #0f4ab2;
      --spacing-xxs: 8px;
      --spacing-xs: 14px;
      --spacing-sm: 20px;
      --spacing-md: 32px;
      --spacing-lg: 56px;
      --spacing-xl: 80px;
    }

    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: 'Segoe UI', system-ui, -apple-system, sans-serif;
      background: #f8f9fa;
      color: #1f2937;
      line-height: 1.6;
    }

    .container {
      max-width: 780px;
      margin: 0 auto;
      background: white;
      border-radius: 24px;
      overflow: hidden;
      box-shadow: 0 12px 48px rgba(0,0,0,0.08);
      min-height: 100vh;
      display: flex;
      flex-direction: column;
    }

    .content {
      padding: 56px 32px 80px;
      flex: 1;
    }

    /* ── Even BIGGER & more dominant header ── */
    h1 {
      font-size: clamp(56px, 11vw, 96px);     /* Much taller & wider feel */
      font-weight: 900;
      line-height: 1.1;                       /* Tighter → feels taller */
      letter-spacing: -1.4px;
      text-align: center;
      margin: 0 auto 80px auto;
      max-width: 96%;
      background: linear-gradient(135deg, var(--primary-dark), var(--primary), #60a5fa, var(--primary-light));
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
      text-shadow: 0 6px 24px rgba(21, 100, 192, 0.28);
      padding: 0 16px;
    }

    h2 {
      font-size: clamp(36px, 7vw, 52px);
      font-weight: 800;
      line-height: 1.2;
      margin: 0 auto 56px;
      max-width: 92%;
      color: #111827;
      text-align: center;
      letter-spacing: -0.6px;
    }

    p {
      margin-bottom: 1.6em;
      font-size: 1.08rem;
      color: #374151;
    }

    /* Extra vertical space around the giant header */
    .content > h1 {
      margin-top: 0;
      margin-bottom: var(--spacing-xl);
    }

    .content > h1 + * {
      margin-top: 0 !important;
    }

    .content > h2 + * {
      margin-top: 48px !important;
    }

    /* Keep tight spacing between non-header elements */
    .hero-img + h1,
    .hero-img + h2,
    .hero-img + p,
    h1 + p,
    h2 + p,
    p + .cta-wrapper,
    .cta-wrapper + h2,
    .cta-wrapper + .hero-img,
    .cta-wrapper + p,
    .form-block + .hero-img,
    .form-block + h2,
    p + h2,
    p + .hero-img,
    p + .form-block,
    .hero-img + .form-block,
    .hero-img + .cta-wrapper {
      margin-top: var(--spacing-xxs) !important;
    }

    .hero-img {
      width: 100%;
      border-radius: 16px;
      box-shadow: 0 12px 32px rgba(0,0,0,0.12);
      margin: var(--spacing-sm) 0 var(--spacing-xs);
    }

    .cta-wrapper {
      text-align: right;
      margin: var(--spacing-xs) 0 var(--spacing-lg);
    }

    .cta {
      display: inline-block;
      padding: 15px 40px;
      font-size: 1.1rem;
      font-weight: 600;
      background: var(--primary);
      color: white;
      border-radius: 999px;
      text-decoration: none;
      box-shadow: 0 5px 16px rgba(21,100,192,0.25);
      transition: all 0.26s ease;
    }

    .cta:hover {
      background: var(--primary-light);
      transform: translateY(-4px);
      box-shadow: 0 12px 36px rgba(21,100,192,0.38);
    }

    .form-block {
      padding: 36px;
      background: #ffffff;
      border-radius: 18px;
      margin: var(--spacing-xl) 0;
      border: 1px solid #e5e7eb;
      box-shadow: 0 5px 20px rgba(0,0,0,0.06);
    }

    .form-block input,
    .form-block button {
      width: 100%;
      padding: 15px 18px;
      margin: var(--spacing-xxs) 0;
      border-radius: 12px;
      border: 1px solid #d1d5db;
      font-size: 1.05rem;
    }

    .form-block button {
      background: var(--primary);
      color: white;
      border: none;
      font-weight: 600;
      cursor: pointer;
      margin-top: var(--spacing-sm);
    }

    .footer-cta {
      padding: 18px 32px;
      text-align: center;
      background: #f8fafc;
      border-top: 1px solid #e5e7eb;
      font-size: 0.86rem;
      color: #6b7280;
    }

    .join-sendmi-link {
      color: #3b82f6;
      text-decoration: underline;
      font-weight: 400;
      font-size: 0.8rem;
      transition: color 0.18s ease;
    }

    .join-sendmi-link:hover,
    .join-sendmi-link:focus {
      color: var(--primary);
      text-decoration-thickness: 2.2px;
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="content">
      <% blocks.forEach(block => { %>
        <% if (block.type === 'text') { %>
          <% 
            let html = block.content
              .replace(/\n{2,}/g, '</p><p>')
              .replace(/\n/g, '<br>');
            
            if (!html.includes('<p')) {
              html = '<p>' + html + '</p>';
            }
          %>

          <% if (block.tag === 'h1') { %>
            <h1><%- html %></h1>
          <% } else if (block.tag === 'h2') { %>
            <h2><%- html %></h2>
          <% } else { %>
            <%- html %>
          <% } %>
        <% } else if (block.type === 'image') { %>
          <img src="<%= block.src %>" alt="Image" class="hero-img" loading="lazy">
        <% } else if (block.type === 'button') { %>
          <div class="cta-wrapper">
            <a href="<%= block.href || '#' %>" class="cta" 
               <%= block.href && block.href.startsWith('http') ? 'target="_blank" rel="noopener"' : '' %>>
              <%= block.text %>
            </a>
          </div>
        <% } else if (block.type === 'form') { %>
          <div class="form-block"><%- block.html %></div>
        <% } %>
      <% }) %>
    </div>

    <div class="footer-cta">
      <a href="https://sendmi.onrender.com" class="join-sendmi-link" target="_blank" rel="noopener">
        Join Sendmi
      </a>
    </div>
  </div>
</body>
</html>
