<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>
  <style>
    :root {
      --primary: #1564C0;
      --primary-light: #3485e5;
      --spacing-xxs: 8px;
      --spacing-xs: 14px;
      --spacing-sm: 20px;
      --spacing-md: 32px;
      --spacing-lg: 48px;
    }

    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    html, body {
      -webkit-text-size-adjust: 100%;
      text-size-adjust: 100%;
    }

    /* Critical fix: apply max-height DIRECTLY to text elements (proven most reliable) */
    h1, h2, p {
      max-height: 10000px !important;  /* Try 5000px or 15000px if this doesn't stick */
    }

    body {
      font-family: 'Segoe UI', system-ui, 'Helvetica Neue', Arial, sans-serif;
      background: #f8f9fa;
      color: #1f2937;
      line-height: 1.6;
    }

    .container {
      max-width: 740px;
      margin: 0 auto;
      background: white;
      border-radius: 20px;
      overflow: hidden;
      box-shadow: 0 10px 40px rgba(0,0,0,0.07);
      min-height: 100vh;
      display: flex;
      flex-direction: column;
    }

    .content {
      padding: 32px 24px 64px;
      flex: 1;
      text-align: center;  /* Helps Chrome treat it as constrained like simple version */
    }

    h1 {
      font-size: 68px !important;          /* Fixed large size – test 60px to 80px on your phone */
      line-height: 1.08;
      font-weight: 900;
      letter-spacing: -1.2px;
      margin: 0 auto 32px;                 /* Tighter bottom margin for stability */
      max-width: 94%;
      padding: 0 16px;
      background: linear-gradient(135deg, var(--primary), var(--primary-light));
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
      text-shadow: 0 4px 14px rgba(0,0,0,0.2);
    }

    h2 {
      font-size: 44px;
      line-height: 1.25;
      font-weight: 700;
      margin: 0 auto 40px;
      max-width: 90%;
      color: #1a1a1a;
    }

    p {
      margin-bottom: 1.6em;
      font-size: 1.05rem;
      color: #374151;
    }

    .content > h1 + *,
    .content > h2 + * {
      margin-top: 28px !important;
    }

    .hero-img {
      width: 100%;
      border-radius: 14px;
      box-shadow: 0 10px 28px rgba(0,0,0,0.11);
      margin: var(--spacing-sm) 0 var(--spacing-xs);
    }

    .cta-wrapper {
      text-align: right;
      margin: var(--spacing-xs) 0 var(--spacing-sm);
    }

    .cta {
      display: inline-block;
      padding: 13px 30px;
      font-size: 1rem;
      font-weight: 600;
      background: var(--primary);
      color: white;
      border-radius: 999px;
      text-decoration: none;
      box-shadow: 0 4px 12px rgba(21,100,192,0.2);
      transition: all 0.22s ease;
    }

    .cta:hover {
      background: var(--primary-light);
      transform: translateY(-2px);
      box-shadow: 0 8px 28px rgba(21,100,192,0.32);
    }

    .form-block {
      padding: 28px;
      background: #ffffff;
      border-radius: 14px;
      margin: var(--spacing-md) 0;
      border: 1px solid #e5e7eb;
      box-shadow: 0 3px 14px rgba(0,0,0,0.04);
    }

    .form-block input,
    .form-block button {
      width: 100%;
      padding: 13px 15px;
      margin: var(--spacing-xxs) 0;
      border-radius: 10px;
      border: 1px solid #d1d5db;
      font-size: 1rem;
    }

    .form-block button {
      background: var(--primary);
      color: white;
      border: none;
      font-weight: 600;
      cursor: pointer;
      margin-top: var(--spacing-xs);
    }

    .footer-cta {
      padding: 12px 28px;
      text-align: center;
      background: #f8fafc;
      border-top: 1px solid #e5e7eb;
      font-size: 0.82rem;
      color: #6b7280;
    }

    .join-sendmi-link {
      color: #3b82f6;
      text-decoration: underline;
      font-weight: 400;
      font-size: 0.75rem;
      transition: color 0.16s ease;
    }

    .join-sendmi-link:hover,
    .join-sendmi-link:focus {
      color: var(--primary);
      text-decoration-thickness: 2px;
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="content">
      <% blocks.forEach(block => { %>
        <% if (block.type === 'text') { %>
          <% 
            let html = block.content
              .replace(/\n{2,}/g, '</p><p>')
              .replace(/\n/g, '<br>');
            
            if (!html.includes('<p')) {
              html = '<p>' + html + '</p>';
            }
          %>

          <% if (block.tag === 'h1') { %>
            <h1><%- html %></h1>
          <% } else if (block.tag === 'h2') { %>
            <h2><%- html %></h2>
          <% } else { %>
            <%- html %>
          <% } %>
        <% } else if (block.type === 'image') { %>
          <img src="<%= block.src %>" alt="Image" class="hero-img" loading="lazy">
        <% } else if (block.type === 'button') { %>
          <div class="cta-wrapper">
            <a href="<%= block.href || '#' %>" class="cta" 
               <%= block.href && block.href.startsWith('http') ? 'target="_blank" rel="noopener"' : '' %>>
              <%= block.text %>
            </a>
          </div>
        <% } else if (block.type === 'form') { %>
          <div class="form-block"><%- block.html %></div>
        <% } %>
      <% }) %>
    </div>

    <div class="footer-cta">
      <a href="https://sendmi.onrender.com" class="join-sendmi-link" target="_blank" rel="noopener">
        Join Sendmi
      </a>
    </div>
  </div>
</body>
</html>
